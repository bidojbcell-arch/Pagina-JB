import Link from "next/link"; import {createClient} from "@/lib/supabase/server"; import {Producto} from "@/lib/types"; import AdminProductList from "@/components/AdminProductList"; import SalesModule from "@/components/SalesModule"; export const revalidate=0; export default async function AdminDashboard(){const s=createClient();const {data}=await s.from("productos").select("*").order("created_at",{ascending:false});const p=(data??[]) as Producto[];return <div className="space-y-6"><div className="flex items-end justify-between"><div><p className="text-xs font-bold uppercase tracking-widest text-accent-600">JBCELL Admin</p><h1 className="text-2xl font-black text-brand-900">Inventario y ventas</h1><p className="text-sm text-slate-500">{p.length} productos · {p.reduce((n,x)=>n+x.stock,0)} unidades en stock</p></div><Link href="/admin/productos/nuevo" className="rounded-xl bg-brand-700 px-5 py-2.5 font-bold text-white">+ Nuevo producto</Link></div><SalesModule productos={p}/><AdminProductList productosIniciales={p}/></div>}